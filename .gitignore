# Build outputs
bin/
obj/

# User-specific files
*.user
*.suo
.vs/

# NuGet
*.nupkg
packages/

# OS
.DS_Store
Thumbs.db

# Secrets — never commit real API keys
appsettings.Development.json
appsettings.Local.json
appsettings.Production.json
local-agent.appsettings.json
publish/
publish-linux/
publish-local-agent/
publish.zip
# LocalAgent ZIP is a 366MB build artifact — never commit it (exceeds GitHub's 100MB limit).
# publish-local-agent.ps1 regenerates it; deploy-gcp.ps1 uploads it to the VM.
/local-agent.zip
local-agent-new.zip
src/SportsMonitor.Bff/wwwroot/downloads/*.zip
